# Puerto del servidor
PORT=3000

# MongoDB Connection String
# Para MongoDB Atlas, usa el formato:
# MONGODB_URI=mongodb+srv://username:password@cluster.mongodb.net/database_name?retryWrites=true&w=majority
# Para MongoDB local:
# MONGODB_URI=mongodb://localhost:27017/miru_db
MONGODB_URI=

# JWT Secret (para autenticación) - Cambia esto en producción
JWT_SECRET=miru_secret_key_2024_change_in_production

# CORS Origin
# CORS_ORIGIN=http://localhost:3000
